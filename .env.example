# Zoom Meeting SDK (frontend-only; no backend needed)
# Copy to .env and add your Zoom SDK credentials from https://marketplace.zoom.us/
VITE_ZOOM_SDK_KEY=your_sdk_key
VITE_ZOOM_SDK_SECRET=your_sdk_secret
